services:
  postgres:
    image: postgres:16
    container_name: interview-lens-db
    environment:
      POSTGRES_USER: interviewlens
      POSTGRES_PASSWORD: dev
      POSTGRES_DB: interviewlens
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
